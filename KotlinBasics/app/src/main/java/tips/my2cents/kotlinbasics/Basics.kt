package tips.my2cents.kotlinbasics

/*
data class CoffeeDetails(
    val sugarCount: Int,
    val name: String,
    val size: String,
    val creamAmount: Int
)


fun makeCoffee(coffeeDetails: CoffeeDetails) {
    var sugarMessage: String
    var creamMessage: String
    when (coffeeDetails.sugarCount) {
        0 -> sugarMessage = "no"
        1 -> sugarMessage = "1 teaspoon of"
        else -> sugarMessage = "${coffeeDetails.sugarCount} teaspoons of"
    }
    when (coffeeDetails.creamAmount) {
        0 -> creamMessage = "no"
        1 -> creamMessage = "1 teaspoon of"
        else -> creamMessage = "${coffeeDetails.creamAmount} teaspoons of"
    }
    println("Coffee with $sugarMessage sugar and $creamMessage cream  for ${coffeeDetails.name}.")
}
*/


fun main() {

}

